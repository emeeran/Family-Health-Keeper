import { Router } from 'express';

const router = Router();

// Placeholder routes - implement as needed
router.get('/', (req, res) => {
  res.json({ success: true, data: [], message: 'Medications endpoint - coming soon' });
});

export default router;